/*
TODO:
	重构步骤
	1）修改图片位置，收到资源文件夹中

	个性化配置，例如从文件中读取配置信息，动态的获得各种参数
	重构Show，把逻辑代码放到外面，不再有show函数控制
	重构Mine类，把其作为Screen的内部类
BUG:

这一步做了什么
创建了一些类，并且继承了Drawable方法，但是发现，对于需要显示数字的类而言
不能明确界定数字处理的相关问题，此外还考虑到可能需要仔细研究一下继承体系的问题
是否需要设置一个Unchecked的类，还是每个类都有一个相关的方法，毕竟每个类都可以翻开
继承一个draw方法，click方法，和mark方法？
貌似这样更加合理	

done:
现在需要优化代码，代码中有大量重复的片段，需要修改
至于显示的问题，看起来是不能一点都不耦合的，所以还是在draw里面判读怎么显示吧
但是按照MVC设计模式，draw应该只负责draw，而不应该考虑数据，所以这里的问题是数据的结构不行
最终方法：draw不判定，只负责绘制指定的图形

从新开始的功能
=>通过读取键盘的输入，从而对游戏进行控制
      需要两个函数，一个负责新开游戏，一个负责当前游戏重开
	  两个函数都需要从构造函数中抽取一部分，同时对于重开游戏，只需要把所有变量的值重置，保留雷的信息即可
	  同时由于游戏运行时间变长，需要考虑资源的释放问题

扫雷秘籍
=>比如输入lizecismydaddy，然后鼠标点击一个位置，可以显示指定位置的周围雷的分布
      至少对于高级模式来说，实在是太难了
	  考虑使用deque，在不断输入的字符中，一旦连续输入了指定序列，即可获得
	  至于输出，这个也可以作为一个函数，最后的显示叠加在外面就可以了

DEBUG:
	使用作弊手段时，已经出现的雷会被重复标记，导致最后的剩余雷数不正常
	增加对雷标记的状态检查

*/